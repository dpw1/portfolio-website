{{> header  title="Diego Fortes | Web Developer"}}

{{> preloader}}

<div class="c-main-container  js-main-container">
  {{> section-header}}
  {{> intro}}
  {{> expertise}}
  {{> experiences}}
  {{> education}}
  {{> portfolio}}
  {{> reviews}}
  {{> section-footer}}
</div>

{{> footer}}
